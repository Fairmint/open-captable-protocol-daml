-- Role helpers for payer, recipient, and processor in approvals and authorization checks.
module CantonPayments.PaymentStream.Types.PartyRole where

-- | Role of a party in a paymentStream
data PartyRole
  = Payer
  | Recipient
  | Processor
  deriving (Eq, Show)
