module CantonPayments.PaymentStream.Types.ProcessorContext where

-- | Processor configuration for payment streams
data ProcessorContext = ProcessorContext
  with
    processor : Party
    dso : Party
  deriving (Eq, Show)
