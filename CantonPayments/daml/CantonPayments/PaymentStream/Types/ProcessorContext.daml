-- Processor party identity and infrastructure CIDs for stream factories.
-- Fits in: PaymentStreamFactory and proposals use this to enforce processor signatory rules.
module CantonPayments.PaymentStream.Types.ProcessorContext where

-- | Processor configuration for payment streams
data ProcessorContext = ProcessorContext
  with
    processor : Party
    dso : Party
  deriving (Eq, Show)
