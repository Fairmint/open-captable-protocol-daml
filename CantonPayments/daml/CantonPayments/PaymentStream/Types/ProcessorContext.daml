-- Processor identity and infrastructure CIDs; PaymentStreamFactory and proposals use it for processor signatory rules.
module CantonPayments.PaymentStream.Types.ProcessorContext where

-- | Processor configuration for payment streams
data ProcessorContext = ProcessorContext
  with
    processor : Party
    dso : Party
  deriving (Eq, Show)
