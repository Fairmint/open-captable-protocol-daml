-- CantonNft interface for one transferable NFT; concrete templates implement it and pair with registry and asset modules.
module Fairmint.OpenCapTableNft.Nft where

import Fairmint.OpenCapTableNft.Types

interface CantonNft where
  viewtype NftView
