-- CantonNft interface for a single transferable NFT representation.
-- Fits in: Implemented by concrete NFT templates; integrates with the registry and asset modules.
module Fairmint.OpenCapTableNft.Nft where

import Fairmint.OpenCapTableNft.Types

interface CantonNft where
  viewtype NftView
