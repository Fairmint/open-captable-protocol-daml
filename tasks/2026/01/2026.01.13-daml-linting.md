# DAML Linting Integration

**Date:** 2026-01-13 **Status:** Complete ✅

## Summary

Investigate and implement DAML linting/formatting in CI to maintain code quality for DAML smart
contracts.

## Implementation

### CI Integration

Added DAML linting to CI workflow (`.github/workflows/ci.yml`):

- Runs `dpm damlc lint` on all 7 DAML packages after build
- **Fails the build** if any lint warnings or errors are found
- Uses GitHub Actions' `::group::` for collapsible output per package
- Reports errors via `::error::` annotations for easy identification

### NPM Script

Added `npm run lint:daml` script in `package.json` for local linting:

```bash
npm run lint:daml  # Lint all DAML packages - exits with error code if warnings found
```

### Code Generator Templates Updated

Fixed the CapTable code generator to produce lint-clean code:

- `scripts/codegen/templates/CapTable.daml.template` - Added `isNone`, `isSome` imports
- `scripts/codegen/templates/loops/create-case.daml` - Use `isNone()` and `isSome()` instead of
  `== None` / `/= None`
- `scripts/codegen/templates/loops/edit-case.daml` - Same pattern fixes
- `scripts/codegen/templates/loops/delete-case.daml` - Same pattern fixes

### Manual Files Fixed (OpenCapTable-v29)

Fixed lint warnings in hand-written DAML files:

- `Types.daml` - Use `optional`, `isSome`, `notElem` patterns
- `ConvertibleConversion.daml` - Use `optional` pattern
- `EquityCompensationIssuance.daml` - Use `optional`, `isSome` patterns
- `StockPlan.daml` - Use `notElem` pattern
- `VestingTerms.daml` - Use `optional` pattern

### CantonPayments Fixed

Fixed all warnings:

- `Airdrop.daml` - Use infix `notElem`, `fromOptional`
- `PersonalAirdrop.daml` - Use `fromOptional`
- `ActivePaymentStream.daml` - Consolidated duplicate imports
- `Approvals.daml` - Use `||` instead of redundant `if`
- `PaymentStreamChanges.daml` - Use `fromOptional`, `optional` patterns
- `PaymentStreamProposalChanges.daml` - Use `fromOptional`, `optional` patterns

### Test Package Fixed

Fixed all warnings:

- `SimpleAirdropTest.daml` - Use `(> 0)` instead of `\n -> n > 0`
- `TestAirdrop.daml` - Use infix `elem`
- `TestStakeholder.daml` - Use `===` from `DA.Assert`
- `TestArchive.daml` - Use `isNone`
- `TestPayerInitiatedLifecycle.daml` - Use `isNone`
- `TestPaymentStreamChanges.daml` - Use `isNone`
- `TestProcessorInitiatedLifecycle.daml` - Use `isNone`
- `TestRecipientInitiatedLifecycle.daml` - Use `isNone`

## Current Lint Status

| Package                          | Status   | Warnings |
| -------------------------------- | -------- | -------- |
| OpenCapTable-v29                 | ✅ Clean | 0        |
| CantonPayments                   | ✅ Clean | 0        |
| OpenCapTableReports-v01          | ✅ Clean | 0        |
| OpenCapTableProofOfOwnership-v01 | ✅ Clean | 0        |
| Shared                           | ✅ Clean | 0        |
| CouponMinter                     | ✅ Clean | 0        |
| Test                             | ✅ Clean | 0        |

**All 7 packages are lint-clean.** CI will fail if any warnings are introduced.

## Research Findings

### Available Tools

1. **`dpm damlc lint`** (Built-in DAML SDK via dpm)
   - Part of the DAML SDK
   - Runs Haskell-style lint checks adapted for DAML (HLint-based)
   - Reports suggestions/warnings at `DsInfo` severity level

2. **DAML IDE Extension**
   - Provides real-time linting in VS Code
   - No CLI/CI integration beyond the built-in lint

3. **No Dedicated DAML Formatter**
   - DAML is based on Haskell syntax
   - Haskell formatters don't have DAML-specific support

## Acceptance Criteria

- [x] `daml lint` runs in CI on all DAML packages
- [x] CI fails if `daml lint` finds errors or warnings
- [x] Document any suppressed warnings with rationale (none suppressed - all fixed)
- [x] Update `llms.txt` with any new coding standards discovered (none needed)

## Coding Patterns Discovered

The linter prefers these patterns:

| Instead of                             | Use                        |
| -------------------------------------- | -------------------------- |
| `x == None`                            | `isNone x`                 |
| `x /= None`                            | `isSome x`                 |
| `notElem x xs`                         | ``x `notElem` xs`` (infix) |
| `elem x xs`                            | ``x `elem` xs`` (infix)    |
| `all (/= "") xs`                       | `"" \`notElem\` xs`        |
| `case x of None -> def; Some v -> v`   | `fromOptional def x`       |
| `case x of None -> def; Some v -> f v` | `optional def f x`         |
| `optional def identity x`              | `fromOptional def x`       |
| `if x then True else y`                | `x \|\| y`                 |
| `\x -> x /= ""`                        | `(/= "")`                  |
| `\n -> n > 0`                          | `(> 0)`                    |
| `assert (x == y)`                      | `x === y` (from DA.Assert) |

## Changelog

| Date       | Change                                                          |
| ---------- | --------------------------------------------------------------- |
| 2026-01-16 | Implemented CI integration and fixed OpenCapTable-v29 warnings  |
| 2026-01-16 | Fixed all CantonPayments and Test warnings - all packages clean |
| 2026-01-16 | Updated CI to fail on lint warnings                             |

_Last updated: 2026-01-16_
