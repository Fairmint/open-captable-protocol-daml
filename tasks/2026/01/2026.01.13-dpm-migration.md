# Task: Migrate from daml to dpm CLI

**Date**: 2026-01-13 **Status**: Complete ✅ **Target Repo**: `open-captable-protocol-daml`

## Goal

Migrate from the deprecated `daml` CLI (Daml Assistant) to `dpm` (Digital Asset Package Manager) as
the primary build tool for DAML packages.

## Background

Per [Canton 3.4 Release Notes](https://blog.digitalasset.com/developers/release-notes/canton-3.4-release-notes-for-splice-0.5.0):

> From version 3.4.x onwards, dpm is the preferred CLI tool for a local development experience...
> As a result of the introduction of dpm, the Daml Assistant (daml) is deprecated as of version 3.4,
> but still can be used to work with 3.3 and 3.4 daml projects.

The `daml` assistant will be **removed in Canton 3.5**, so migration is required before upgrading.

## Current State (After Migration)

| Location           | Uses `dpm` | Command                                   |
| ------------------ | ---------- | ----------------------------------------- |
| `package.json`     | ✅         | `dpm build`, `dpm clean`                  |
| `package.json`     | ✅         | `dpm test`, `dpm codegen-js`              |
| `.github/ci.yml`   | ✅         | Installs to `~/.dpm/bin`                  |
| Developer machines | ✅         | `dpm` commands (add `~/.dpm/bin` to PATH) |

## Command Migration Map

Per [dpm documentation](https://docs.digitalasset.com/build/3.4/dpm/dpm.html#operate):

| Current (`daml`)   | New (`dpm`)                | Notes                       |
| ------------------ | -------------------------- | --------------------------- |
| `daml build`       | `dpm build`                | ✅ Direct replacement       |
| `daml build --all` | `dpm build` (in each dir)  | ⚠️ No `--all` flag in dpm   |
| `daml test`        | `dpm test`                 | ✅ Direct replacement       |
| `daml clean`       | `dpm clean`                | ✅ Direct replacement       |
| `daml clean --all` | `dpm clean` (in each dir)  | ⚠️ No `--all` flag in dpm   |
| `daml codegen js`  | `dpm codegen-js`           | ✅ Direct replacement       |
| `daml studio`      | `dpm studio`               | ✅ Direct replacement       |
| `daml install`     | `dpm install`              | ✅ Direct replacement       |

### New Alpha Codegens (Future Investigation)

| Command                        | Description                         |
| ------------------------------ | ----------------------------------- |
| `dpm codegen-alpha-typescript` | New alpha TypeScript codegen        |
| `dpm codegen-alpha-java`       | New alpha Java codegen              |
| `dpm codegen-alpha-scala`      | New alpha Scala codegen             |

The `codegen-alpha-typescript` may provide better TypeScript types without `@daml/ledger` dependency.

## Implementation Plan

### Phase 1: Verify dpm Works with Existing Setup

- [x] Add `~/.dpm/bin` to PATH
- [x] Test `dpm build` in `OpenCapTable-v28/` directory
- [x] Test `dpm test` in `Test/` directory
- [x] Test `dpm codegen-js` works as expected
- [x] Verify `daml.yaml` files work unchanged with dpm

### Phase 2: Handle `--all` Flag Migration

**Discovery:** `dpm build` automatically handles multi-package dependencies! When run from any package
directory, it reads `multi-package.yaml` and builds all dependencies first. No custom script needed.

**Key fix:** Added `Test` to `multi-package.yaml` (was missing, caused build errors).

### Phase 3: Update package.json Scripts

Updated scripts to use `dpm` commands:
- `build`: `cd Test && dpm build` (builds all deps via multi-package.yaml)
- `test`: `cd Test && dpm test ...`
- `codegen`: Uses `dpm codegen-js` for each package
- `clean`: Loop through packages with `dpm clean`

### Phase 4: Update CI Workflow

Updated `.github/workflows/ci.yml`:
- Changed PATH from `~/.daml/bin` to `~/.dpm/bin`
- Removed `--install-assistant=yes` flag (not needed for dpm)
- Updated step name to indicate dpm

### Phase 5: Documentation

- [x] Update llms.txt with dpm commands and "Build Tooling" section
- [x] Update task file with completion status

## `@daml/ledger` Deprecation

### Current Situation

The repo uses `@daml/ledger` (v2.10.3) as a dependency:

```json
{
  "dependencies": {
    "@daml/ledger": "2.10.3",
    "@daml/types": "3.4.10"
  }
}
```

Per release notes: _"The deprecated @daml/ledger and @daml/react typescript libraries are no longer
distributed with Canton 3.4."_

### Impact Assessment

1. **Generated JS bindings** - The `daml codegen js` / `dpm codegen-js` output imports from
   `@daml/ledger` and `@daml/types`
2. **bundle-dependencies.ts** - Creates stubs that import from `@daml/ledger`
3. **Downstream SDK consumers** - `ocp-canton-sdk` uses the generated types

### Mitigation Options

1. **Continue using 2.x versions** - The `@daml/ledger@2.10.3` package should still be available on
   npm as it was published before 3.4. Monitor for security issues.

2. **Investigate `dpm codegen-alpha-typescript`** - The new alpha TypeScript codegen may generate
   code without `@daml/ledger` dependency. Test if viable.

3. **Use JSON Ledger API directly** - Replace `@daml/ledger` with direct HTTP calls to the JSON
   Ledger API v2. The `canton-node-sdk` already uses this approach.

4. **Create custom type definitions** - If the generated types are minimal, we could maintain our
   own type definitions without `@daml/ledger`.

### Recommended Path

For now, keep `@daml/ledger@2.10.3` as it still works. Plan to investigate
`dpm codegen-alpha-typescript` as part of a future Canton 3.5 upgrade.

## Success Criteria

- [x] All builds succeed with `dpm` commands
- [x] All tests pass with `dpm test`
- [x] TypeScript codegen works with `dpm codegen-js`
- [x] CI pipeline uses dpm and passes
- [x] No runtime dependency on deprecated libraries (or documented path forward)

## Risks

| Risk                                  | Impact | Mitigation                              |
| ------------------------------------- | ------ | --------------------------------------- |
| dpm doesn't support `--all` flag      | Medium | Create build scripts for multi-package  |
| `@daml/ledger` npm package removed    | High   | Pin version, investigate alpha codegen  |
| CI changes break builds               | Medium | Test in branch before merging           |

## Validation Commands

```bash
# Verify dpm is installed and in PATH
dpm version

# Test build
cd OpenCapTable-v28 && dpm build

# Test codegen
cd OpenCapTable-v28 && dpm codegen-js

# Test test runner
cd Test && dpm test
```

## References

- [dpm Documentation](https://docs.digitalasset.com/build/3.4/dpm/dpm.html)
- [Canton 3.4 Release Notes](https://blog.digitalasset.com/developers/release-notes/canton-3.4-release-notes-for-splice-0.5.0)
- [JSON Ledger API Migration Guide](https://docs.digitalasset.com/build/3.4/sdlc-howtos/migration/index.html)

---

## Changelog

| Date       | Change                                                   | PR  |
| ---------- | -------------------------------------------------------- | --- |
| 2026-01-13 | Created task file                                        | —   |
| 2026-01-13 | Completed migration: package.json, CI, multi-package.yaml | —   |

---

_Last updated: 2026-01-13_
