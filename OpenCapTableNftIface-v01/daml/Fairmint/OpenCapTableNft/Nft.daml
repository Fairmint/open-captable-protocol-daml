-- CantonNft interface for one transferable NFT; implementations live in OpenCapTableNft-v01 (separate package for upgradeability).
module Fairmint.OpenCapTableNft.Nft where

import Fairmint.OpenCapTableNft.Types

interface CantonNft where
  viewtype NftView
