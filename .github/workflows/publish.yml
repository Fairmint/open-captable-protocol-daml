name: Publish to NPM

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  publish:
    runs-on: ubuntu-latest
    if: github.ref == 'refs/heads/main'
    permissions:
      contents: write
      id-token: write

    steps:
      - name: Checkout code
        uses: actions/checkout@v7
        with:
          fetch-depth: 0
          token: ${{ secrets.GITHUB_TOKEN }}
          submodules: false

      - name: Setup Git LFS
        run: |
          git lfs install
          git lfs pull

      - name: Setup Node.js
        uses: actions/setup-node@v7
        with:
          node-version: '22'
          registry-url: 'https://registry.npmjs.org'

      - name: Upgrade npm
        run: npm install -g npm@11.10.0

      - name: Setup Java
        uses: actions/setup-java@v5
        with:
          distribution: 'temurin'
          java-version: '23'

      - name: Install dependencies
        run: npm i

      - name: Restore Daml SDK cache
        uses: actions/cache@v6
        with:
          path: ~/.dpm
          key: dpm-sdk-${{ runner.os }}-${{ hashFiles('**/daml.yaml', 'package.json') }}
          restore-keys: |
            dpm-sdk-${{ runner.os }}-

      - name: Install DAML SDK (dpm)
        run: |
          test -x ./node_modules/.bin/canton-dev-tools
          npm exec canton-dev-tools -- install-dpm-sdks
          echo "$HOME/.dpm/bin" >> "$GITHUB_PATH"

      - name: Build DAML packages
        run: npm run build

      - name: Generate TypeScript bindings
        run: npm run codegen

      - name: Prepare release
        run: npm run prepare-release

      - name: Publish to NPM
        run: npm publish

      - name: Create and push git tag
        run: |
          # Get the new version from package.json
          NEW_VERSION=$(node -p "require('./package.json').version")

          # Create git tag
          git config --local user.email "action@github.com"
          git config --local user.name "GitHub Action"
          git tag -a "v$NEW_VERSION" -m "Release v$NEW_VERSION"
          git push origin "v$NEW_VERSION"
